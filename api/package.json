{
  "name": "copy-stats-api",
  "version": "1.0.0",
  "description": "API for tracking install script copies",
  "main": "copy-stats.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241025.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
